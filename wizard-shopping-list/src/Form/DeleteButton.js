import styled from 'styled-components'
import Button from '../Button/Button'

export default function DeleteButton({onDeleteAll}) {
    <Button type="submit" text="Delete All" onClick={onDeleteAll}/>
} 
